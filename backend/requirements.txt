fastapi>=0.110.0
uvicorn[standard]>=0.29.0
faster-whisper>=1.0.0
ollama>=0.1.9
pyttsx3>=2.90
python-multipart>=0.0.9
websockets>=12.0
numpy>=1.26.0
pydantic-settings>=2.2.0
boto3>=1.34.0

# ── Optional: GPU/CUDA support for faster-whisper ────────────────────────────
# Uncomment these two lines if WHISPER_DEVICE=gpu/cuda to install the required
# CUDA 12 runtime DLLs without a full CUDA Toolkit install.
# After uncommenting, run: pip install -r requirements.txt
nvidia-cublas-cu12
nvidia-cudnn-cu12
